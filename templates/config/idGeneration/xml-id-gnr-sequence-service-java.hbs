package {{defaultPackageName}}.config;

import org.egovframe.rte.fdl.idgnr.impl.EgovSequenceIdGnrService;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import javax.sql.DataSource;

@Configuration
public class {{txtFileName}} {

  private final DataSource dataSource;

  public {{txtFileName}}(DataSource dataSource) {
    this.dataSource = dataSource;
  }

  @Bean(name = "{{txtIdServiceName}}")
  public EgovSequenceIdGnrService {{txtIdServiceName}}() {
    EgovSequenceIdGnrService sequenceIdGnrService = new EgovSequenceIdGnrService();
    sequenceIdGnrService.setDataSource(dataSource);
    sequenceIdGnrService.setQuery("{{txtQuery}}");
    {{#if (eq rdoType "BigDecimal")}}
    sequenceIdGnrService.setUseBigDecimals(true);
    {{/if}}
    return sequenceIdGnrService;
  }
}
