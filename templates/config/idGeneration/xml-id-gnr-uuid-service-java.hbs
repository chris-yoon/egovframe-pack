package {{defaultPackageName}}.config;

import org.egovframe.rte.fdl.idgnr.impl.EgovUUIdGnrService;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class {{txtFileName}} {

    @Bean(name = "{{txtIdServiceName}}")
    public EgovUUIdGnrService {{txtIdServiceName}}() {
        EgovUUIdGnrService uuidGnrService = new EgovUUIdGnrService();
        {{#if (eq rdoType "Address")}}
        uuidGnrService.setAddress("{{txtAddress}}");
        {{/if}}
        return uuidGnrService;
    }
}
