package {{defaultPackageName}}.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.scheduling.quartz.SchedulerFactoryBean;
import org.springframework.scheduling.quartz.CronTriggerBean;

@Configuration
public class {{txtFileName}} {

    @Bean(name = "{{txtSchedulerName}}")
    public SchedulerFactoryBean {{txtSchedulerName}}(CronTriggerBean {{txtTriggerName}}) {
        SchedulerFactoryBean schedulerFactoryBean = new SchedulerFactoryBean();
        schedulerFactoryBean.setTriggers({{txtTriggerName}});
        return schedulerFactoryBean;
    }
}
